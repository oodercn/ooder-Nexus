# 场景管理模块重新设计

**版本**: 1.0  
**日期**: 2026-02-21  
**状态**: 设计中

---

## 1. 设计背景

### 1.1 现状分析

当前场景管理模块存在以下问题：

1. **场景创建能力不足**：仅支持预定义场景，用户无法创建自定义场景
2. **场景分类不清晰**：主场场景、协作场景、可配置Skills混在一起
3. **配置入口分散**：Skills配置分散在多个页面，用户体验不佳
4. **状态展示不直观**：无法直观看到已连接服务的运行状况

### 1.2 设计目标

1. 支持SDK用户创建自定义场景
2. 清晰区分主场场景、协作场景、可配置Skills
3. 统一配置入口，提升用户体验
4. 实时展示服务运行状态

---

## 2. 场景分类体系

### 2.1 三层场景架构

```
┌─────────────────────────────────────────────────────────────┐
│                      场景分类体系                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              1. 主场场景 (Primary Scenes)            │   │
│  │  - 系统核心场景，不可删除                            │   │
│  │  - VFS、Auth、Msg、Workflow、A2UI                   │   │
│  │  - 提供基础能力支撑                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │            2. 协作场景 (Collaboration Scenes)        │   │
│  │  - 用户创建的场景组                                  │   │
│  │  - 支持多人协作                                      │   │
│  │  - 可邀请成员、分配角色                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │            3. 可配置Skills (Configurable Skills)     │   │
│  │  - 已安装的第三方技能                                │   │
│  │  - 需要配置才能使用                                  │   │
│  │  - 如：钉钉、飞书、企业微信等                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 场景类型定义

| 类型 | 标识 | 说明 | 可删除 | 可配置 |
|------|------|------|--------|--------|
| 主场场景 | `primary` | 系统核心场景 | 否 | 是 |
| 协作场景 | `collaboration` | 用户创建的场景组 | 是 | 是 |
| 可配置Skills | `skill` | 第三方技能 | 是 | 是 |

---

## 3. 用户故事

### 3.1 新建场景用户故事

#### US-SCENE-001: 创建自定义场景

**作为** SDK用户  
**我希望** 能够创建自定义场景  
**以便** 根据业务需求定义专属场景

**验收标准**:
- 支持输入场景名称、描述
- 支持选择场景类型（协作场景）
- 支持定义场景能力需求
- 支持设置角色定义
- 创建成功后自动生成场景ID

#### US-SCENE-002: 定义场景能力

**作为** 场景创建者  
**我希望** 能够为场景定义所需能力  
**以便** 明确场景需要哪些技能支持

**验收标准**:
- 支持从能力库中选择已有能力
- 支持定义能力参数约束
- 支持设置能力优先级
- 支持添加自定义能力描述

#### US-SCENE-003: 配置场景角色

**作为** 场景创建者  
**我希望** 能够为场景定义角色  
**以便** 控制成员权限和职责

**验收标准**:
- 支持定义角色名称和描述
- 支持为角色分配能力权限
- 支持设置角色层级
- 预设角色：所有者、管理员、成员、访客

#### US-SCENE-004: 发布场景

**作为** 场景创建者  
**我希望** 能够发布场景供他人使用  
**以便** 实现场景共享和协作

**验收标准**:
- 支持设置场景可见性（私有/公开）
- 支持生成场景邀请码
- 支持设置加入审批流程
- 发布后场景状态变为"已发布"

#### US-SCENE-005: 管理场景生命周期

**作为** 场景所有者  
**我希望** 能够管理场景的生命周期  
**以便** 控制场景的运行状态

**验收标准**:
- 支持启动/停止场景
- 支持暂停/恢复场景
- 支持归档场景
- 支持删除场景（需确认）

---

## 4. API 设计

### 4.1 场景定义 API

#### 创建场景定义

```
POST /api/scene/definition/create
```

**请求体**:
```json
{
  "name": "我的协作场景",
  "description": "用于团队协作的自定义场景",
  "type": "collaboration",
  "category": "business",
  "capabilities": [
    {
      "capId": "vfs-read",
      "required": true,
      "priority": 1
    },
    {
      "capId": "msg-send",
      "required": true,
      "priority": 2
    }
  ],
  "roles": [
    {
      "roleName": "管理员",
      "roleCode": "admin",
      "permissions": ["read", "write", "admin"],
      "level": 10
    },
    {
      "roleName": "成员",
      "roleCode": "member",
      "permissions": ["read", "write"],
      "level": 5
    }
  ],
  "visibility": "private"
}
```

**响应**:
```json
{
  "requestStatus": 200,
  "message": "场景创建成功",
  "data": {
    "sceneId": "scene-collab-001",
    "name": "我的协作场景",
    "status": "CREATED",
    "inviteCode": "INV-ABC123",
    "createdAt": "2026-02-21T10:00:00Z"
  }
}
```

#### 获取场景定义列表

```
POST /api/scene/definition/list
```

**请求体**:
```json
{
  "type": "collaboration",
  "status": "PUBLISHED",
  "page": 1,
  "size": 20
}
```

#### 更新场景定义

```
POST /api/scene/definition/update
```

**请求体**:
```json
{
  "sceneId": "scene-collab-001",
  "name": "更新后的场景名称",
  "description": "更新后的描述",
  "capabilities": [],
  "roles": []
}
```

#### 删除场景定义

```
POST /api/scene/definition/delete
```

**请求体**:
```json
{
  "sceneId": "scene-collab-001",
  "confirm": true
}
```

### 4.2 场景实例 API

#### 创建场景实例

```
POST /api/scene/instance/create
```

**请求体**:
```json
{
  "sceneId": "scene-collab-001",
  "instanceName": "项目A协作组",
  "config": {
    "maxMembers": 50,
    "heartbeatInterval": 30000
  }
}
```

**响应**:
```json
{
  "requestStatus": 200,
  "data": {
    "instanceId": "inst-001",
    "sceneId": "scene-collab-001",
    "instanceName": "项目A协作组",
    "status": "CREATED",
    "inviteCode": "GRP-XYZ789",
    "ownerId": "user-001"
  }
}
```

#### 场景生命周期管理

```
POST /api/scene/instance/{instanceId}/start
POST /api/scene/instance/{instanceId}/stop
POST /api/scene/instance/{instanceId}/pause
POST /api/scene/instance/{instanceId}/resume
POST /api/scene/instance/{instanceId}/archive
```

### 4.3 本地场景 API

#### 获取本地场景概览

```
GET /api/scene/local/overview
```

**响应**:
```json
{
  "requestStatus": 200,
  "data": {
    "primaryScenes": {
      "total": 5,
      "active": 5,
      "scenes": [
        {
          "sceneId": "vfs",
          "name": "Virtual File System",
          "status": "RUNNING",
          "capabilities": 10,
          "icon": "ri-folder-line"
        }
      ]
    },
    "collaborationScenes": {
      "total": 3,
      "active": 2,
      "scenes": [
        {
          "instanceId": "inst-001",
          "name": "项目A协作组",
          "members": 5,
          "status": "RUNNING",
          "role": "owner"
        }
      ]
    },
    "configurableSkills": {
      "total": 4,
      "configured": 2,
      "skills": [
        {
          "skillId": "skill-org-dingding",
          "name": "钉钉组织服务",
          "status": "CONFIGURED",
          "connected": true
        }
      ]
    }
  }
}
```

---

## 5. 界面设计

### 5.1 场景管理主页面

```
┌─────────────────────────────────────────────────────────────────────┐
│  场景管理                                              [+ 新建场景]  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ 主场场景 │ │ 协作场景 │ │ 可配置   │ │ 场景组   │               │
│  │    5     │ │    3     │ │ Skills 4 │ │    2     │               │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘               │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  [全部] [运行中] [已停止] [待配置]          [搜索场景...]   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                                                             │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │   │
│  │  │ 📁 VFS      │  │ 🔐 Auth     │  │ 💬 Msg      │         │   │
│  │  │ 运行中      │  │ 运行中      │  │ 运行中      │         │   │
│  │  │ 10 能力     │  │ 7 能力      │  │ 5 能力      │         │   │
│  │  │ [必需]      │  │ [必需]      │  │ [可选]      │         │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘         │   │
│  │                                                             │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │   │
│  │  │ 🔄 Workflow │  │ 🎨 A2UI     │  │ 👥 协作场景 │         │   │
│  │  │ 运行中      │  │ 运行中      │  │ 我的项目组  │         │   │
│  │  │ 7 能力      │  │ 5 能力      │  │ 5 成员      │         │   │
│  │  │ [可选]      │  │ [可选]      │  │ [所有者]    │         │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘         │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 新建场景页面

```
┌─────────────────────────────────────────────────────────────────────┐
│  新建场景                                                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  步骤指示: [1. 基本信息] → [2. 能力定义] → [3. 角色配置] → [4. 发布]│
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  基本信息                                                    │   │
│  │                                                             │   │
│  │  场景名称 *                                                 │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ 我的协作场景                                         │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  场景描述                                                   │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ 用于团队协作的自定义场景...                          │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  场景类型 *                                                 │   │
│  │  ○ 协作场景 (多人协作，支持邀请成员)                       │   │
│  │  ○ 个人场景 (仅自己使用)                                   │   │
│  │                                                             │   │
│  │  场景分类                                                   │   │
│  │  [业务 ▼]                                                   │   │
│  │                                                             │   │
│  │                              [取消]  [下一步：能力定义]     │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3 能力定义页面

```
┌─────────────────────────────────────────────────────────────────────┐
│  新建场景 - 能力定义                                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  已选能力 (3)                                                │   │
│  │                                                             │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ 📁 vfs-read    [必需] 优先级: 1     [移除]          │   │   │
│  │  │    读取文件内容                                       │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ 📝 vfs-write   [必需] 优先级: 2     [移除]          │   │   │
│  │  │    写入文件内容                                       │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ 💬 msg-send    [可选] 优先级: 3     [移除]          │   │   │
│  │  │    发送消息                                           │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  [+ 添加能力]                                               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  可用能力库                                                  │   │
│  │                                                             │   │
│  │  [搜索能力...]                                              │   │
│  │                                                             │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │   │
│  │  │ vfs-read  │ │ vfs-write │ │ vfs-delete│ │ vfs-list  │   │   │
│  │  │ [添加]    │ │ [添加]    │ │ [添加]    │ │ [添加]    │   │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│                              [上一步]  [取消]  [下一步：角色配置]   │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.4 本地场景展示页面

```
┌─────────────────────────────────────────────────────────────────────┐
│  本地场景                                                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  主场场景 (5)                                    [查看全部]  │   │
│  │                                                             │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐│   │
│  │  │ 📁 VFS  │ │ 🔐 Auth │ │ 💬 Msg  │ │ 🔄 WF   │ │ 🎨 A2UI││   │
│  │  │ 运行中  │ │ 运行中  │ │ 运行中  │ │ 运行中  │ │ 运行中 ││   │
│  │  │ ● 10 能力│ │ ● 7 能力 │ │ ● 5 能力 │ │ ● 7 能力 │ │ ● 5 能力││   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └────────┘│   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  协作场景 (3)                           [+ 创建协作场景]     │   │
│  │                                                             │   │
│  │  ┌───────────────────────────────────────────────────────┐ │   │
│  │  │ 📋 项目A协作组                          [所有者]       │ │   │
│  │  │ 状态: 运行中  |  成员: 5人  |  创建: 2026-02-20        │ │   │
│  │  │ 邀请码: GRP-XYZ789                     [管理] [邀请]  │ │   │
│  │  └───────────────────────────────────────────────────────┘ │   │
│  │  ┌───────────────────────────────────────────────────────┐ │   │
│  │  │ 📋 技术分享小组                          [成员]       │ │   │
│  │  │ 状态: 运行中  |  成员: 12人  |  创建: 2026-02-15       │ │   │
│  │  │ 邀请码: GRP-ABC123                     [查看] [退出]  │ │   │
│  │  └───────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  可配置 Skills (4)                            [技能市场]    │   │
│  │                                                             │   │
│  │  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐     │   │
│  │  │ 📱 钉钉       │ │ 📱 飞书       │ │ 📱 企业微信   │     │   │
│  │  │ ● 已配置      │ │ ● 已配置      │ │ ○ 待配置      │     │   │
│  │  │ 已连接        │ │ 已连接        │ │ 未连接        │     │   │
│  │  │ [配置] [停用] │ │ [配置] [停用] │ │ [配置]        │     │   │
│  │  └───────────────┘ └───────────────┘ └───────────────┘     │   │
│  │                                                             │   │
│  │  ┌───────────────┐                                         │   │
│  │  │ 🗄️ 数据库连接 │                                         │   │
│  │  │ ○ 待配置      │                                         │   │
│  │  │ 未连接        │                                         │   │
│  │  │ [配置]        │                                         │   │
│  │  └───────────────┘                                         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6. 数据模型

### 6.1 场景定义模型

```java
public class SceneDefinition {
    private String sceneId;
    private String name;
    private String description;
    private String type;           // primary, collaboration, skill
    private String category;       // infrastructure, security, business, etc.
    private String version;
    private boolean required;
    private List<CapabilityRef> capabilities;
    private List<RoleDefinition> roles;
    private String visibility;     // private, public
    private String status;         // CREATED, PUBLISHED, ARCHIVED
    private String ownerId;
    private Date createdAt;
    private Date updatedAt;
}

public class CapabilityRef {
    private String capId;
    private String name;
    private boolean required;
    private int priority;
    private Map<String, Object> constraints;
}

public class RoleDefinition {
    private String roleCode;
    private String roleName;
    private String description;
    private List<String> permissions;
    private int level;
}
```

### 6.2 场景实例模型

```java
public class SceneInstance {
    private String instanceId;
    private String sceneId;
    private String instanceName;
    private String status;         // CREATED, RUNNING, PAUSED, STOPPED, ARCHIVED
    private String ownerId;
    private List<SceneMember> members;
    private String inviteCode;
    private SceneConfig config;
    private Date createdAt;
    private Date updatedAt;
}

public class SceneMember {
    private String memberId;
    private String memberName;
    private String role;
    private Date joinedAt;
    private boolean online;
    private Date lastHeartbeat;
}

public class SceneConfig {
    private int maxMembers;
    private long heartbeatInterval;
    private boolean approvalRequired;
    private Map<String, Object> customConfig;
}
```

---

## 7. 实现计划

### 7.1 阶段一：场景定义管理

| 任务 | 说明 | 优先级 |
|------|------|--------|
| SceneDefinitionController | 场景定义CRUD API | P0 |
| SceneDefinitionService | 场景定义业务逻辑 | P0 |
| scene-create.html | 新建场景页面 | P0 |
| scene-definition-list.html | 场景定义列表页面 | P1 |

### 7.2 阶段二：场景实例管理

| 任务 | 说明 | 优先级 |
|------|------|--------|
| SceneInstanceController | 场景实例API | P0 |
| SceneInstanceService | 场景实例业务逻辑 | P0 |
| 集成SceneEngine SDK | 使用SDK创建场景组 | P0 |

### 7.3 阶段三：本地场景展示

| 任务 | 说明 | 优先级 |
|------|------|--------|
| local-scenes.html | 本地场景展示页面 | P0 |
| LocalSceneController | 本地场景概览API | P0 |
| 场景状态实时更新 | WebSocket推送 | P1 |

---

**版本**: 1.0  
**日期**: 2026-02-21  
**作者**: ooder Team
