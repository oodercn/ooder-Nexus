# Skills 配置展现设计

**版本**: 1.0  
**日期**: 2026-02-21  
**状态**: 设计中

---

## 1. 设计背景

### 1.1 现状分析

当前Skills配置存在以下问题：

1. **配置入口分散**：钉钉、飞书、企业微信等配置分散在不同页面
2. **配置状态不直观**：无法直观看到哪些Skills已配置、哪些待配置
3. **连接状态缺失**：无法看到Skills与外部服务的连接状态
4. **数据库配置缺失**：缺少数据库连接配置入口

### 1.2 设计目标

1. 统一Skills配置入口
2. 清晰展示配置状态和连接状态
3. 支持多种Skills类型配置（组织服务、数据库等）
4. 提供配置验证和测试功能

---

## 2. Skills 分类

### 2.1 按类型分类

| 类型 | 标识 | Skills示例 | 配置项 |
|------|------|-----------|--------|
| 组织服务 | `org` | 钉钉、飞书、企业微信 | AppKey、AppSecret、API地址 |
| 数据库 | `database` | MySQL、PostgreSQL、H2 | 连接串、用户名、密码 |
| 消息服务 | `messaging` | 钉钉消息、飞书消息 | Webhook、签名密钥 |
| 存储服务 | `storage` | OSS、S3、MinIO | AccessKey、SecretKey、Bucket |
| AI服务 | `ai` | OpenAI、Claude | API Key、模型配置 |

### 2.2 按状态分类

| 状态 | 标识 | 说明 |
|------|------|------|
| 未安装 | `NOT_INSTALLED` | Skill未安装 |
| 待配置 | `PENDING_CONFIG` | 已安装但未配置 |
| 已配置 | `CONFIGURED` | 已配置但未验证连接 |
| 已连接 | `CONNECTED` | 配置验证通过，可正常使用 |
| 连接失败 | `CONNECTION_FAILED` | 配置验证失败 |
| 已停用 | `DISABLED` | 用户主动停用 |

---

## 3. 用户故事

### 3.1 组织服务配置

#### US-SKILL-CONFIG-001: 配置钉钉组织服务

**作为** 系统管理员  
**我希望** 能够配置钉钉组织服务  
**以便** 实现钉钉用户同步和消息推送

**验收标准**:
- 支持输入AppKey和AppSecret
- 支持配置API地址（可选，默认官方地址）
- 支持测试连接验证配置正确性
- 配置成功后显示连接状态

#### US-SKILL-CONFIG-002: 配置飞书组织服务

**作为** 系统管理员  
**我希望** 能够配置飞书组织服务  
**以便** 实现飞书用户同步和消息推送

**验收标准**:
- 支持输入App ID和App Secret
- 支持配置API地址（可选）
- 支持测试连接验证配置正确性

#### US-SKILL-CONFIG-003: 配置企业微信

**作为** 系统管理员  
**我希望** 能够配置企业微信服务  
**以便** 实现企业微信用户同步和消息推送

**验收标准**:
- 支持输入企业ID、Agent ID和Secret
- 支持测试连接验证配置正确性

### 3.2 数据库配置

#### US-SKILL-CONFIG-004: 配置数据库连接

**作为** 系统管理员  
**我希望** 能够配置外部数据库连接  
**以便** 实现数据集成和查询

**验收标准**:
- 支持选择数据库类型（MySQL、PostgreSQL等）
- 支持输入连接串、用户名、密码
- 支持测试连接验证配置正确性
- 支持配置连接池参数

#### US-SKILL-CONFIG-005: 管理多个数据库连接

**作为** 系统管理员  
**我希望** 能够管理多个数据库连接  
**以便** 连接不同的数据源

**验收标准**:
- 支持添加多个数据库连接
- 支持为连接命名和描述
- 支持启用/停用连接
- 支持删除连接

---

## 4. API 设计

### 4.1 Skills 配置 API

#### 获取Skills配置概览

```
GET /api/skills/config/overview
```

**响应**:
```json
{
  "requestStatus": 200,
  "data": {
    "total": 6,
    "configured": 3,
    "connected": 2,
    "pendingConfig": 2,
    "connectionFailed": 1,
    "categories": [
      {
        "type": "org",
        "name": "组织服务",
        "skills": [
          {
            "skillId": "skill-org-dingding",
            "name": "钉钉组织服务",
            "icon": "ri-dingding-line",
            "status": "CONNECTED",
            "lastChecked": "2026-02-21T10:00:00Z",
            "configFields": ["DINGDING_APP_KEY", "DINGDING_APP_SECRET"]
          },
          {
            "skillId": "skill-org-feishu",
            "name": "飞书组织服务",
            "icon": "ri-lark-line",
            "status": "CONFIGURED",
            "lastChecked": "2026-02-21T09:00:00Z"
          }
        ]
      },
      {
        "type": "database",
        "name": "数据库连接",
        "skills": [
          {
            "skillId": "skill-db-mysql",
            "name": "MySQL连接",
            "icon": "ri-database-2-line",
            "status": "PENDING_CONFIG"
          }
        ]
      }
    ]
  }
}
```

#### 获取Skill配置详情

```
GET /api/skills/config/{skillId}
```

**响应**:
```json
{
  "requestStatus": 200,
  "data": {
    "skillId": "skill-org-dingding",
    "name": "钉钉组织服务",
    "version": "1.0.0",
    "description": "钉钉组织数据集成服务",
    "status": "CONNECTED",
    "configSchema": {
      "required": [
        {
          "name": "DINGDING_APP_KEY",
          "type": "string",
          "label": "App Key",
          "description": "钉钉应用的AppKey"
        },
        {
          "name": "DINGDING_APP_SECRET",
          "type": "password",
          "label": "App Secret",
          "description": "钉钉应用的AppSecret",
          "secret": true
        }
      ],
      "optional": [
        {
          "name": "DINGDING_API_BASE_URL",
          "type": "string",
          "label": "API地址",
          "default": "https://oapi.dingtalk.com",
          "description": "钉钉API地址"
        }
      ]
    },
    "currentConfig": {
      "DINGDING_APP_KEY": "dingxxxxxx",
      "DINGDING_APP_SECRET": "******",
      "DINGDING_API_BASE_URL": "https://oapi.dingtalk.com"
    },
    "connectionInfo": {
      "connected": true,
      "lastChecked": "2026-02-21T10:00:00Z",
      "responseTime": 150,
      "error": null
    }
  }
}
```

#### 更新Skill配置

```
POST /api/skills/config/{skillId}/update
```

**请求体**:
```json
{
  "config": {
    "DINGDING_APP_KEY": "dingxxxxxx",
    "DINGDING_APP_SECRET": "mysecret",
    "DINGDING_API_BASE_URL": "https://oapi.dingtalk.com"
  },
  "testConnection": true
}
```

**响应**:
```json
{
  "requestStatus": 200,
  "message": "配置保存成功",
  "data": {
    "skillId": "skill-org-dingding",
    "status": "CONNECTED",
    "connectionTest": {
      "success": true,
      "responseTime": 150,
      "message": "连接成功"
    }
  }
}
```

#### 测试Skill连接

```
POST /api/skills/config/{skillId}/test
```

**请求体**:
```json
{
  "config": {
    "DINGDING_APP_KEY": "dingxxxxxx",
    "DINGDING_APP_SECRET": "mysecret"
  }
}
```

**响应**:
```json
{
  "requestStatus": 200,
  "data": {
    "success": true,
    "responseTime": 150,
    "message": "连接成功",
    "details": {
      "apiVersion": "1.0",
      "corpName": "示例企业"
    }
  }
}
```

### 4.2 数据库连接 API

#### 获取数据库连接列表

```
GET /api/skills/database/connections
```

**响应**:
```json
{
  "requestStatus": 200,
  "data": {
    "connections": [
      {
        "connectionId": "db-conn-001",
        "name": "主数据库",
        "type": "mysql",
        "host": "192.168.1.100",
        "port": 3306,
        "database": "nexus",
        "status": "CONNECTED",
        "lastChecked": "2026-02-21T10:00:00Z"
      }
    ]
  }
}
```

#### 创建数据库连接

```
POST /api/skills/database/connections/create
```

**请求体**:
```json
{
  "name": "主数据库",
  "type": "mysql",
  "host": "192.168.1.100",
  "port": 3306,
  "database": "nexus",
  "username": "root",
  "password": "password",
  "poolConfig": {
    "maxPoolSize": 10,
    "minIdle": 2,
    "connectionTimeout": 30000
  }
}
```

#### 测试数据库连接

```
POST /api/skills/database/connections/test
```

**请求体**:
```json
{
  "type": "mysql",
  "host": "192.168.1.100",
  "port": 3306,
  "database": "nexus",
  "username": "root",
  "password": "password"
}
```

---

## 5. 界面设计

### 5.1 Skills配置概览页面

```
┌─────────────────────────────────────────────────────────────────────┐
│  Skills 配置                                                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ 总数 6   │ │ 已配置 3 │ │ 已连接 2 │ │ 待配置 2 │               │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘               │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  组织服务 (3)                                                │   │
│  │                                                             │   │
│  │  ┌─────────────────┐ ┌─────────────────┐ ┌───────────────┐ │   │
│  │  │ 📱 钉钉         │ │ 📱 飞书         │ │ 📱 企业微信   │ │   │
│  │  │ ● 已连接        │ │ ○ 已配置        │ │ ○ 待配置      │ │   │
│  │  │ 响应: 150ms     │ │ 响应: --        │ │ 未配置        │ │   │
│  │  │                 │ │                 │ │               │ │   │
│  │  │ [配置] [停用]   │ │ [配置] [测试]   │ │ [配置]        │ │   │
│  │  └─────────────────┘ └─────────────────┘ └───────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  数据库连接 (2)                              [+ 新建连接]   │   │
│  │                                                             │   │
│  │  ┌───────────────────────────────────────────────────────┐ │   │
│  │  │ 🗄️ 主数据库 (MySQL)                      ● 已连接     │ │   │
│  │  │ 192.168.1.100:3306/nexus                              │ │   │
│  │  │ 响应: 5ms                              [配置] [测试]  │ │   │
│  │  └───────────────────────────────────────────────────────┘ │   │
│  │  ┌───────────────────────────────────────────────────────┐ │   │
│  │  │ 🗄️ 备份数据库 (PostgreSQL)               ○ 待配置    │ │   │
│  │  │ 192.168.1.101:5432/nexus_backup                      │ │   │
│  │  │ 未配置                                 [配置] [删除]  │ │   │
│  │  └───────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  消息服务 (1)                                                │   │
│  │                                                             │   │
│  │  ┌─────────────────┐                                       │   │
│  │  │ 💬 钉钉消息     │                                       │   │
│  │  │ ○ 待配置        │                                       │   │
│  │  │ [配置]          │                                       │   │
│  │  └─────────────────┘                                       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 Skill配置详情页面

```
┌─────────────────────────────────────────────────────────────────────┐
│  钉钉组织服务配置                                    [返回列表]     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  基本信息                                                    │   │
│  │                                                             │   │
│  │  名称: 钉钉组织服务                                         │   │
│  │  版本: 1.0.0                                                │   │
│  │  状态: ● 已连接                                             │   │
│  │  最后检查: 2026-02-21 10:00:00                              │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  必填配置                                                    │   │
│  │                                                             │   │
│  │  App Key *                                                  │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ dingxxxxxxxxxxxx                                     │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │  钉钉应用的AppKey                                          │   │
│  │                                                             │   │
│  │  App Secret *                                               │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ ••••••••••••••••••                                   │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │  钉钉应用的AppSecret                                       │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  可选配置                                                    │   │
│  │                                                             │   │
│  │  API地址                                                    │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ https://oapi.dingtalk.com                            │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │  钉钉API地址，默认为官方地址                               │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  连接状态                                                    │   │
│  │                                                             │   │
│  │  ● 已连接                                                   │   │
│  │  响应时间: 150ms                                            │   │
│  │  企业名称: 示例企业                                         │   │
│  │  API版本: 1.0                                               │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│                    [测试连接]  [取消]  [保存配置]                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3 数据库连接配置页面

```
┌─────────────────────────────────────────────────────────────────────┐
│  新建数据库连接                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  基本信息                                                    │   │
│  │                                                             │   │
│  │  连接名称 *                                                 │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ 主数据库                                             │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  数据库类型 *                                               │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ MySQL ▼                                              │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  描述                                                       │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ 生产环境主数据库                                     │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  连接配置                                                    │   │
│  │                                                             │   │
│  │  主机地址 *               端口 *                            │   │
│  │  ┌───────────────────┐   ┌───────────┐                     │   │
│  │  │ 192.168.1.100     │   │ 3306      │                     │   │
│  │  └───────────────────┘   └───────────┘                     │   │
│  │                                                             │   │
│  │  数据库名 *                                                 │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ nexus                                                │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  用户名 *                                                   │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ root                                                 │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  密码 *                                                     │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │ ••••••••                                              │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  连接池配置 (高级)                              [展开 ▼]   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│                    [测试连接]  [取消]  [创建连接]                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6. 数据模型

### 6.1 Skill配置模型

```java
public class SkillConfig {
    private String skillId;
    private String name;
    private String type;
    private String version;
    private String description;
    private String status;
    private ConfigSchema configSchema;
    private Map<String, Object> currentConfig;
    private ConnectionInfo connectionInfo;
    private Date lastUpdated;
}

public class ConfigSchema {
    private List<ConfigField> required;
    private List<ConfigField> optional;
}

public class ConfigField {
    private String name;
    private String type;        // string, password, number, boolean
    private String label;
    private String description;
    private boolean secret;
    private Object defaultValue;
    private List<String> options;
}

public class ConnectionInfo {
    private boolean connected;
    private Date lastChecked;
    private long responseTime;
    private String error;
    private Map<String, Object> details;
}
```

### 6.2 数据库连接模型

```java
public class DatabaseConnection {
    private String connectionId;
    private String name;
    private String description;
    private String type;        // mysql, postgresql, oracle, etc.
    private String host;
    private int port;
    private String database;
    private String username;
    private String password;
    private PoolConfig poolConfig;
    private String status;
    private Date lastChecked;
    private Date createdAt;
    private Date updatedAt;
}

public class PoolConfig {
    private int maxPoolSize;
    private int minIdle;
    private long connectionTimeout;
    private long idleTimeout;
    private long maxLifetime;
}
```

---

## 7. 实现计划

### 7.1 阶段一：Skills配置框架

| 任务 | 说明 | 优先级 |
|------|------|--------|
| SkillConfigController | Skills配置API | P0 |
| SkillConfigService | 配置业务逻辑 | P0 |
| skills-config.html | Skills配置页面 | P0 |

### 7.2 阶段二：组织服务配置

| 任务 | 说明 | 优先级 |
|------|------|--------|
| 钉钉配置实现 | AppKey/Secret配置和验证 | P0 |
| 飞书配置实现 | App ID/Secret配置和验证 | P1 |
| 企业微信配置实现 | CorpID/AgentID/Secret配置 | P1 |

### 7.3 阶段三：数据库连接

| 任务 | 说明 | 优先级 |
|------|------|--------|
| DatabaseConnectionController | 数据库连接API | P0 |
| DatabaseConnectionService | 连接管理逻辑 | P0 |
| database-config.html | 数据库配置页面 | P0 |
| 连接测试功能 | JDBC连接测试 | P0 |

---

**版本**: 1.0  
**日期**: 2026-02-21  
**作者**: ooder Team
