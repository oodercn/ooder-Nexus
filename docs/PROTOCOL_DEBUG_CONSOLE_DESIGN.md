# 协议调试控制台功能设计文档

## 1. 概述

### 1.1 产品定位
协议调试控制台是一个用于测试、调试和验证南向协议的工具系统，提供协议查询、仿真模拟、数据存储等功能，帮助开发者快速验证协议实现和调试问题。

### 1.2 核心功能
1. **协议查询** - 查看和搜索已注册的协议
2. **协议仿真** - 创建和管理模拟器
3. **实时模拟** - 启动仿真器进行真实测试
4. **数据存储** - 本地JSON存储协议数据
5. **管理界面** - 左侧菜单独立入口

---

## 2. 功能模块设计

### 2.1 模块架构

```
┌─────────────────────────────────────────────────────────────┐
│                    协议调试控制台                            │
├─────────────────────────────────────────────────────────────┤
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌─────────┐│
│  │ 协议查询   │  │ 模拟器管理 │  │ 仿真执行   │  │ 数据存储 ││
│  │ Module   │  │ Module   │  │ Module   │  │ Module  ││
│  └───────────┘  └───────────┘  └───────────┘  └─────────┘│
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐  │
│  │              公共模拟器框架 (Plugin System)            │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │  McpSimulator  │  RouteSimulator  │  EndSimulator  │  │
│  └─────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐  │
│  │              本地存储引擎 (Local Storage)             │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │  protocols.json  │  simulators.json  │  results.json  │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 功能详细设计

#### 2.2.1 协议查询模块

**功能描述**: 查看和搜索已注册的协议信息

**核心功能**:
| 功能 | 说明 | 优先级 |
|------|------|--------|
| 协议列表 | 显示所有已注册的协议类型 | P0 |
| 协议详情 | 查看协议的详细信息 | P0 |
| 命令列表 | 查看协议支持的命令类型 | P0 |
| 协议搜索 | 按名称/类型搜索协议 | P1 |
| 统计信息 | 显示协议使用统计 | P1 |

**数据结构**:

```json
// 协议信息结构
{
  "protocolId": "MCP-001",
  "protocolType": "MCP",
  "name": "Master Control Protocol",
  "description": "主控制协议，用于管理MCP节点",
  "version": "2.0",
  "status": "ACTIVE",
  "supportedCommands": [
    {
      "commandId": "MCP_REGISTER",
      "name": "节点注册",
      "description": "注册新的MCP节点",
      "parameters": [
        {"name": "nodeId", "type": "String", "required": true},
        {"name": "nodeName", "type": "String", "required": false},
        {"name": "capabilities", "type": "Map", "required": false}
      ],
      "responseFormat": {
        "code": "Integer",
        "message": "String",
        "nodeId": "String"
      }
    }
  ],
  "statistics": {
    "totalCommands": 1523,
    "successRate": 99.2,
    "avgResponseTime": 5.2
  },
  "createdAt": "2026-01-01T00:00:00Z",
  "updatedAt": "2026-02-09T12:00:00Z"
}
```

#### 2.2.2 模拟器管理模块

**功能描述**: 创建、编辑和管理协议模拟器

**核心功能**:
| 功能 | 说明 | 优先级 |
|------|------|--------|
| 模拟器创建 | 创建新的模拟器实例 | P0 |
| 模拟器配置 | 配置模拟器参数 | P0 |
| 模拟器列表 | 查看所有模拟器 | P0 |
| 模拟器启停 | 启动/停止模拟器 | P0 |
| 模拟器导入导出 | 导出导入JSON配置 | P1 |
| 公共模拟器库 | 预设常用模拟器模板 | P1 |

**模拟器类型**:

```java
// 模拟器类型枚举
public enum SimulatorType {
    MCP_NODE("MCP节点模拟器", "模拟MCP主控节点行为"),
    ROUTE_NODE("路由节点模拟器", "模拟路由节点转发行为"),
    END_DEVICE("终端设备模拟器", "模拟终端设备数据上报"),
    GATEWAY("网关模拟器", "模拟协议网关桥接功能"),
    SENSOR("传感器模拟器", "模拟传感器数据采集"),
    ACTUATOR("执行器模拟器", "模拟执行器控制指令")
}

// 模拟器配置结构
{
  "simulatorId": "SIM-MCP-001",
  "name": "MCP节点模拟器-01",
  "type": "MCP_NODE",
  "protocolType": "MCP",
  "description": "用于测试MCP协议注册流程",
  "config": {
    "nodeId": "SIM-NODE-001",
    "nodeName": "模拟MCP节点",
    "autoRegister": true,
    "heartbeatInterval": 5000,
    "statusReportInterval": 10000,
    "capabilities": ["sensing", "control", "communication"]
  },
  "behavior": {
    "autoRegister": true,
    "respondToQueries": true,
    "generateHeartbeat": true,
    "generateStatus": true,
    "randomDelay": {
      "enabled": true,
      "minMs": 100,
      "maxMs": 500
    }
  },
  "scenarios": [
    {
      "scenarioId": "SCENARIO-001",
      "name": "正常注册场景",
      "description": "模拟节点正常注册流程",
      "steps": [
        {"action": "REGISTER", "delay": 1000},
        {"action": "WAIT", "duration": 2000},
        {"action": "HEARTBEAT", "repeat": 3}
      ]
    }
  ],
  "status": "STOPPED",
  "createdAt": "2026-02-09T00:00:00Z"
}
```

#### 2.2.3 仿真执行模块

**功能描述**: 启动仿真器执行测试场景

**核心功能**:
| 功能 | 说明 | 优先级 |
|------|------|--------|
| 场景执行 | 执行预设测试场景 | P0 |
| 单步调试 | 单步执行调试 | P1 |
| 实时监控 | 监控仿真执行状态 | P0 |
| 结果记录 | 记录执行结果 | P0 |
| 日志查看 | 查看详细执行日志 | P1 |
| 中断控制 | 中断正在执行的仿真 | P0 |

**执行流程**:

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  用户    │───>│ 选择    │───>│ 加载     │───>│ 执行    │
│  启动    │    │ 场景    │    │ 模拟器   │    │ 步骤    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                      │
┌──────────┐    ┌──────────┐    ┌──────────┐          │
│  查看    │<───│ 记录    │<───│ 发送    │<──────────┘
│  结果    │    │ 结果    │    │ 命令    │
└──────────┘    └──────────┘    └──────────┘
```

**执行结果结构**:

```json
{
  "executionId": "EXEC-20260209-001",
  "simulatorId": "SIM-MCP-001",
  "scenarioId": "SCENARIO-001",
  "status": "COMPLETED",
  "startTime": "2026-02-09T15:00:00Z",
  "endTime": "2026-02-09T15:00:30Z",
  "duration": 30000,
  "steps": [
    {
      "stepId": 1,
      "action": "REGISTER",
      "request": {
        "protocolType": "MCP",
        "commandType": "MCP_REGISTER",
        "payload": {"nodeId": "SIM-NODE-001"}
      },
      "response": {
        "code": 200,
        "message": "Success",
        "data": {"nodeId": "SIM-NODE-001", "status": "REGISTERED"}
      },
      "timestamp": "2026-02-09T15:00:01Z",
      "duration": 15,
      "success": true
    }
  ],
  "summary": {
    "totalSteps": 10,
    "successSteps": 10,
    "failedSteps": 0,
    "avgResponseTime": 12.5
  }
}
```

#### 2.2.4 数据存储模块

**功能描述**: 本地JSON文件存储协议数据

**存储结构**:

```
storage/
├── protocols/
│   ├── index.json              # 协议索引
│   ├── mcp/
│   │   ├── protocol.json       # MCP协议定义
│   │   └── commands/           # 命令定义
│   │       ├── mcp_register.json
│   │       ├── mcp_heartbeat.json
│   │       └── ...
│   ├── route/
│   │   └── ...
│   └── end/
│       └── ...
├── simulators/
│   ├── index.json             # 模拟器索引
│   └── user/
│       ├── simulator-001.json
│       └── simulator-002.json
├── scenarios/
│   ├── index.json
│   └── mcp/
│       ├── scenario-001.json
│       └── scenario-002.json
└── results/
    ├── index.json
    └── executions/
        ├── exec-001.json
        └── exec-002.json
```

**存储文件示例**:

```json
// storage/protocols/index.json
{
  "lastUpdated": "2026-02-09T12:00:00Z",
  "protocols": [
    {"id": "MCP", "name": "Master Control Protocol", "version": "2.0"},
    {"id": "ROUTE", "name": "Route Protocol", "version": "2.0"},
    {"id": "END", "name": "End Protocol", "version": "2.0"}
  ]
}

// storage/simulators/user/simulator-001.json
{
  "simulatorId": "SIM-MCP-001",
  "name": "MCP节点模拟器",
  "type": "MCP_NODE",
  "config": {
    "nodeId": "SIM-NODE-001",
    "heartbeatInterval": 5000
  },
  "createdAt": "2026-02-09T00:00:00Z",
  "updatedAt": "2026-02-09T00:00:00Z"
}
```

---

## 3. 前端UI设计

### 3.1 左侧菜单结构

```
左侧菜单:
├── 首页 (Home)
├── 协议调试控制台                    ← 新增
│   ├── 协议查询
│   ├── 模拟器管理
│   ├── 仿真执行
│   └── 数据存储
├── 节点管理 (Node Management)
├── 网络拓扑 (Network Topology)
├── 技能市场 (Skill Marketplace)
├── 系统配置 (System Settings)
└── 关于 (About)
```

### 3.2 页面布局

#### 3.2.1 协议查询页面

```
┌─────────────────────────────────────────────────────────────┐
│  协议查询                                                   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐  │
│  │ 搜索框      │ │ 协议类型    │ │ 刷新按钮            │  │
│  └─────────────┘ └─────────────┘ └─────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐    │
│  │  协议卡片列表                                       │    │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐│    │
│  │  │  MCP    │ │  ROUTE   │ │  END    │ │  + 添加 ││    │
│  │  │  协议   │ │  协议   │ │  协议   │ │  自定义 ││    │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘│    │
│  └─────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐    │
│  │  协议详情面板 (选中协议时显示)                       │    │
│  │  - 协议名称                                        │    │
│  │  - 支持的命令列表                                   │    │
│  │  - 统计信息                                        │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

#### 3.2.2 模拟器管理页面

```
┌─────────────────────────────────────────────────────────────┐
│  模拟器管理                                                 │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 工具栏: [新建模拟器] [导入] [导出] [删除]            │    │
│  └─────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────┐ ┌─────────────────────────┐    │
│  │ 模拟器列表              │ │ 模拟器配置面板         │    │
│  │                         │ │                         │    │
│  │  🔧 MCP模拟器-01        │ │  模拟器配置            │    │
│  │     状态: 运行中        │ │  ─────────────────    │    │
│  │     协议: MCP           │ │  节点ID: SIM-001      │    │
│  │                         │ │  心跳间隔: 5000ms     │    │
│  │  🔧 MCP模拟器-02        │ │  自动注册: ✓          │    │
│  │     状态: 停止          │ │                         │    │
│  │     协议: MCP           │ │  [保存] [启动] [停止]  │    │
│  │                         │ │                         │    │
│  │  🔧 Route模拟器-01      │ │  预设场景              │    │
│  │     状态: 停止          │ │  ─────────────────    │    │
│  │     协议: ROUTE         │ │  场景1: 正常注册       │    │
│  │                         │ │  场景2: 心跳测试       │    │
│  │  📦 公共模拟器库        │ │  场景3: 压力测试       │    │
│  │     ───────────────     │ │                         │    │
│  │  📦 MCP标准模拟器       │ │  [添加场景]            │    │
│  │  📦 Route路由模拟器     │ │                         │    │
│  │  📦 End设备模拟器       │ │                         │    │
│  └─────────────────────────┘ └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

#### 3.2.3 仿真执行页面

```
┌─────────────────────────────────────────────────────────────┐
│  仿真执行                                                   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 选择模拟器: [MCP模拟器-01 ▼]                        │    │
│  │ 选择场景:   [正常注册场景 ▼]    [▶ 开始] [⏸ 暂停] │    │
│  └─────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────┐ ┌─────────────────────────┐    │
│  │ 执行日志                 │ │ 执行结果                │    │
│  │                         │ │                         │    │
│  │ [15:00:01] REG → 发送   │ │ 步骤: 1/10             │    │
│  │ [15:00:02] ← 200 OK    │ │ 成功: 10               │    │
│  │ [15:00:03] HB → 发送   │ │ 失败: 0                │    │
│  │ [15:00:04] ← 200 OK    │ │ 耗时: 5.2s             │    │
│  │ ...                     │ │                         │    │
│  │                         │ │ 详细结果                │    │
│  │                         │ │ ─────────────────────   │    │
│  │                         │ │ ✓ REG成功               │    │
│  │                         │ │ ✓ HB成功                │    │
│  │                         │ │ ✓ ST成功                │    │
│  └─────────────────────────┘ └─────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 后端API设计

### 4.1 API端点

| 方法 | 路径 | 功能 | 优先级 |
|------|------|------|--------|
| GET | `/api/protocols` | 获取协议列表 | P0 |
| GET | `/api/protocols/{type}` | 获取协议详情 | P0 |
| GET | `/api/protocols/{type}/commands` | 获取命令列表 | P0 |
| GET | `/api/simulators` | 获取模拟器列表 | P0 |
| POST | `/api/simulators` | 创建模拟器 | P0 |
| GET | `/api/simulators/{id}` | 获取模拟器详情 | P0 |
| PUT | `/api/simulators/{id}` | 更新模拟器 | P0 |
| DELETE | `/api/simulators/{id}` | 删除模拟器 | P0 |
| POST | `/api/simulators/{id}/start` | 启动模拟器 | P0 |
| POST | `/api/simulators/{id}/stop` | 停止模拟器 | P0 |
| POST | `/api/simulators/{id}/execute` | 执行场景 | P0 |
| GET | `/api/executions/{id}` | 获取执行结果 | P0 |
| GET | `/api/storage/protocols` | 获取存储的协议 | P1 |
| GET | `/api/storage/simulators` | 获取存储的模拟器 | P1 |

### 4.2 API详细设计

#### 4.2.1 协议查询API

```java
// 获取协议列表
@RestController
@RequestMapping("/api/protocols")
public class ProtocolController {
    
    @GetMapping
    public ResponseEntity<List<ProtocolInfo>> getProtocols(
            @RequestParam(required = false) String search,
            @RequestParam(required = false) String type) {
        // 返回协议列表
    }
    
    @GetMapping("/{type}")
    public ResponseEntity<ProtocolDetail> getProtocolDetail(@PathVariable String type) {
        // 返回协议详情
    }
    
    @GetMapping("/{type}/commands")
    public ResponseEntity<List<CommandInfo>> getCommands(@PathVariable String type) {
        // 返回命令列表
    }
    
    @GetMapping("/{type}/statistics")
    public ResponseEntity<ProtocolStatistics> getStatistics(@PathVariable String type) {
        // 返回统计信息
    }
}
```

#### 4.2.2 模拟器管理API

```java
// 模拟器管理控制器
@RestController
@RequestMapping("/api/simulators")
public class SimulatorController {
    
    @GetMapping
    public ResponseEntity<List<SimulatorInfo>> getSimulators() {
        // 返回模拟器列表
    }
    
    @PostMapping
    public ResponseEntity<SimulatorInfo> createSimulator(@RequestBody SimulatorConfig config) {
        // 创建模拟器
    }
    
    @PostMapping("/{id}/start")
    public ResponseEntity<ExecutionResult> startSimulator(@PathVariable String id) {
        // 启动模拟器
    }
    
    @PostMapping("/{id}/execute")
    public ResponseEntity<ExecutionResult> executeScenario(
            @PathVariable String id,
            @RequestBody ExecutionRequest request) {
        // 执行场景
    }
}
```

### 4.3 数据传输对象 (DTO)

```java
// 协议信息DTO
public class ProtocolInfo {
    private String protocolId;
    private String protocolType;
    private String name;
    private String description;
    private String version;
    private String status;
    private int commandCount;
    private Statistics summary;
}

// 模拟器配置DTO
public class SimulatorConfig {
    private String simulatorId;
    private String name;
    private SimulatorType type;
    private String protocolType;
    private Map<String, Object> config;
    private List<ScenarioConfig> scenarios;
}

// 执行请求DTO
public class ExecutionRequest {
    private String simulatorId;
    private String scenarioId;
    private Map<String, Object> parameters;
    private boolean async;
}

// 执行结果DTO
public class ExecutionResult {
    private String executionId;
    private String status;
    private long startTime;
    private long endTime;
    private List<StepResult> steps;
    private ExecutionSummary summary;
}
```

---

## 5. 模拟器框架设计

### 5.1 模拟器接口

```java
// 模拟器接口
public interface ProtocolSimulator {
    
    // 获取模拟器ID
    String getSimulatorId();
    
    // 获取模拟器类型
    SimulatorType getType();
    
    // 获取支持的协议类型
    String getProtocolType();
    
    // 初始化模拟器
    void initialize(SimulatorConfig config);
    
    // 启动模拟器
    void start();
    
    // 停止模拟器
    void stop();
    
    // 执行单个命令
    CommandResult execute(CommandPacket command);
    
    // 执行场景
    ExecutionResult executeScenario(Scenario scenario);
    
    // 获取模拟器状态
    SimulatorStatus getStatus();
    
    // 获取执行日志
    List<ExecutionLog> getLogs();
}
```

### 5.2 公共模拟器实现

#### 5.2.1 MCP节点模拟器

```java
@Component
public class McpNodeSimulator implements ProtocolSimulator {
    
    @Override
    public CommandResult execute(CommandPacket command) {
        switch (command.getCommandType()) {
            case "MCP_REGISTER":
                return handleRegister(command);
            case "MCP_HEARTBEAT":
                return handleHeartbeat(command);
            case "MCP_STATUS":
                return handleStatus(command);
            default:
                return CommandResult.error(command.getCommandId(), 
                    400, "Unknown command: " + command.getCommandType());
        }
    }
    
    private CommandResult handleRegister(CommandPacket command) {
        Map<String, Object> payload = command.getPayload();
        String nodeId = (String) payload.get("nodeId");
        
        // 模拟注册延迟
        simulateDelay();
        
        // 注册节点
        registeredNodes.put(nodeId, System.currentTimeMillis());
        
        return CommandResult.success(command.getCommandId(), 
            Map.of("nodeId", nodeId, "status", "REGISTERED"));
    }
    
    private CommandResult handleHeartbeat(CommandPacket command) {
        // 模拟心跳响应
        return CommandResult.success(command.getCommandId(),
            Map.of("status", "ALIVE", "timestamp", System.currentTimeMillis()));
    }
}
```

#### 5.2.2 常用场景配置

```json
// 场景1: 节点注册流程
{
  "scenarioId": "mcp-register-flow",
  "name": "MCP节点注册流程",
  "description": "模拟MCP节点的标准注册流程",
  "protocolType": "MCP",
  "steps": [
    {"action": "REGISTER", "description": "节点注册", "delay": 1000},
    {"action": "WAIT", "duration": 2000},
    {"action": "HEARTBEAT", "description": "首次心跳", "delay": 500},
    {"action": "STATUS", "description": "状态上报", "delay": 1000},
    {"action": "HEARTBEAT", "description": "周期性心跳", "repeat": 5, "interval": 1000}
  ]
}

// 场景2: 压力测试
{
  "scenarioId": "mcp-stress-test",
  "name": "MCP压力测试",
  "description": "模拟高并发注册场景",
  "protocolType": "MCP",
  "config": {
    "concurrentUsers": 10,
    "requestsPerUser": 100,
    "rampUpPeriod": 60
  },
  "steps": [
    {"action": "REGISTER", "description": "批量注册", "batchSize": 50},
    {"action": "PARALLEL_HEARTBEAT", "description": "并发心跳", "concurrent": 20},
    {"action": "STATUS", "description": "状态查询", "repeat": 100}
  ]
}
```

---

## 6. 实施计划

### Phase 1: 基础功能 (Week 1)

- [ ] 创建前端页面结构
- [ ] 实现协议查询API
- [ ] 实现模拟器CRUD API
- [ ] 实现基础UI界面
- [ ] 本地JSON存储基础功能

### Phase 2: 仿真功能 (Week 2)

- [ ] 实现模拟器执行引擎
- [ ] 实现场景配置和执行
- [ ] 实现执行结果记录
- [ ] 实现实时日志显示
- [ ] 实现公共模拟器模板

### Phase 3: 高级功能 (Week 3)

- [ ] 实现单步调试功能
- [ ] 实现导入导出功能
- [ ] 实现统计分析
- [ ] 实现历史记录查询
- [ ] 优化用户体验

### Phase 4: 完善和测试 (Week 4)

- [ ] 单元测试
- [ ] 集成测试
- [ ] 性能测试
- [ ] 文档完善
- [ ] Bug修复

---

## 7. 依赖技术栈

| 组件 | 技术选择 | 说明 |
|------|----------|------|
| 前端框架 | Vue.js 3 + Element Plus | 现代化UI组件库 |
| 状态管理 | Pinia | Vue 3状态管理 |
| 网络请求 | Axios | HTTP客户端 |
| 实时通信 | WebSocket | 实时日志推送 |
| 后端框架 | Spring Boot 2.7 | Java后端框架 |
| JSON处理 | Jackson | JSON序列化 |
| 本地存储 | 文件系统 + Jackson | JSON文件存储 |
| 定时任务 | Spring Scheduler | 仿真调度 |

---

## 8. 风险评估

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 性能瓶颈 | 高并发仿真可能影响系统性能 | 限制并发数，异步处理 |
| 数据一致 | 存储读写可能不一致 | 使用事务，备份机制 |
| 用户体验 | 功能复杂可能影响易用性 | 分阶段上线，收集反馈 |
| 兼容性 | 不同浏览器兼容性 | 主流浏览器测试 |

---

## 9. 成功指标

| 指标 | 目标值 | 测量方式 |
|------|--------|----------|
| 功能完整性 | 100%功能上线 | 功能清单检查 |
| 响应时间 | < 2秒 | API响应时间监控 |
| 用户满意度 | > 90% | 用户反馈调查 |
| 稳定性 | 99.9%可用性 | 运行时间监控 |

---

**文档版本**: 1.0  
**创建日期**: 2026-02-09  
**作者**: ooderNexus开发团队
